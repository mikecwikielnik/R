starting_weights <- c(145, 122, 127, 245, 362, 152)
ending_weights <- c(125, 128, 117, 205, 300, 173)
weightloss<-starting_weights- ending_weights
sum(weightloss)
mean(weightloss)

#it was a challenge in codio
#weightloss is the difference between starting weights and ending weights
#we proceed to take the sum and mean of the weightloss vector
